[pytest]
testpaths = tests
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
markers =
    integration: integration tests (SQLite + reconciler/API, external deps mocked)
    acceptance: acceptance-criteria regression tests from README
filterwarnings =
    ignore::DeprecationWarning