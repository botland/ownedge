# === Appliance Identity ===
APPLIANCE_ID=inferedge-dev-001
COMPOSE_PROJECT_NAME=inferedge

# === Hardware Profile ===
GPU_PROFILE=auto
DEFAULT_MODEL=meta-llama/Llama-3.1-8B-Instruct
DEFAULT_CONTEXT=8192
GPU_UTILIZATION=0.85

# === Credentials ===
MINIO_ROOT_USER=inferedge
MINIO_ROOT_PASSWORD=change-me-in-production
CONTROLLER_API_TOKEN=change-me-in-production

# === Controller ===
CONTROLLER_PORT=8080
# Ray object store: % of host RAM for controller /dev/shm (Ray recommends >30%)
CONTROLLER_SHM_PCT=40
# CONTROLLER_SHM_SIZE_GB is auto-written by scripts/resolve-shm.sh (via scripts/compose.sh)
SQLITE_DB_PATH=/data/inferedge.db
# Host path bind-mounted to /models_cache inside containers (do not use host path as LOCAL_MODEL_CACHE)
MODEL_CACHE_HOST=/var/lib/inferedge/models

# === Ray (single-node for Phase 1) ===
RAY_HEAD_PORT=10001
RAY_DASHBOARD_PORT=8265

# === vLLM (managed by Controller) ===
VLLM_INTERNAL_PORT=8000
VLLM_IMAGE=vllm/vllm-openai:latest
VLLM_CONTAINER_STARTUP_TIMEOUT_SEC=120
VLLM_PROBE_TIMEOUT_SEC=600
VLLM_IMAGE_PULL_TIMEOUT_SEC=3600
VLLM_IMAGE_PULL_STALL_SEC=300
VLLM_IMAGE_PULL_LOG_MIN_BYTES_MB=50
VLLM_SHM_SIZE_GB=4
VLLM_START_TIMEOUT_SEC=3900
ENSURE_ARTIFACT_TIMEOUT_SEC=7500

# === Auto-heal watchdog ===
HEAL_CHECK_INTERVAL_SEC=30
HEAL_STALE_DOWNLOAD_SEC=3900
HEAL_STALE_STARTING_SEC=600
HEAL_STALE_LOADING_SEC=720

# === Reconciler ===
RECONCILE_INTERVAL_SEC=5
HF_DOWNLOAD_TIMEOUT_SEC=7200
HF_DOWNLOAD_STALL_SEC=3600
DOWNLOAD_CONNECTIONS=16
MIN_FREE_DISK_GB=20

# === HF Token (required for gated models e.g. meta-llama/*) ===
# Create at https://huggingface.co/settings/tokens and accept model license on the model page.
HF_TOKEN=