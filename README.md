# OwnEdge / InferEdge

- [`inferedge-phase1/`](inferedge-phase1/) — inference appliance backend (controller, vLLM, LiteLLM)
- [`appliance-console/`](appliance-console/) — standalone web UI mock for appliance management
- `usb-dongle-*.sh` — USB configuration dongle scripts (bootstrap/recovery)